include <stdio.h>


int main()
{
   char s[30];
   printf("\n enter the string\n");
   scanf("%s",&s);
   int length=0;
   for (int i=0;s[i]!='\0';i++)
   {
       length++;
   }
   printf("\n the length of the string =%d ",length);

}
